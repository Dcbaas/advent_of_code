#ifndef SONAR_H
#define SONAR_H

#include <span>
#include <string>

namespace ch1 {
	int count_depth_increases(std::string& file_path);
	int count_depth_window_increases(std::string& file_path);
}
#endif // !SONAR_H
